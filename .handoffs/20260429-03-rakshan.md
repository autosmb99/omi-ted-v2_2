# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 14 (third 04-29 session — kept name 03-rakshan to match request)
- **Machine**: rakshan / cowork-sandbox
- **Date/time started**: 2026-04-29 IST
- **Date/time ended**: 2026-04-29 IST
- **Context tokens used (approx)**: ~35%
- **Model used**: claude-opus-4-7

## Repo State
- **Branch**: main
- **Last commit SHA**: (run `git rev-parse HEAD` after this push)
- **Last commit message**: deploy: railway always-on configs (auto-migrate, retry, proxy)
- **Uncommitted changes**: see `git status --short` — none after this push
- **Stash entries**: none

## Local Environment State

Local skipped this session as a temporary exception for Session #14:
Rakshan reported repeated `backend not connected`, `yt-dlp 529`, and
`network error` on Windows. Before any future Railway/Vercel deploy, verify
`uvicorn main:app --reload` locally first and reopen Windows debugging with
repro steps for those three errors.

- **Backend running**: no (cloud only)
- **Frontend running**: no (cloud only)
- **DB in use**: Postgres on Railway (managed plugin) - provisioned; configure via DEPLOY.md step 3 if recreating it
- **Migration version**: auto-applied on every boot via Dockerfile CMD
- **Row counts**: 0 (Postgres fresh)
- **LLM provider in config.yaml**: sarvam
- **Sentry/PostHog connected**: no

## What I Just Completed

### Switched project from local-first to always-on Railway

Files touched:

| File | Change |
|------|--------|
| `backend/Dockerfile` | Context-relative COPY (`COPY requirements.txt`, `COPY .`). CMD now `alembic upgrade head && uvicorn ...` so migrations run on every boot. PYTHONUNBUFFERED=1, workers=2. Railway service Root Directory must be set to `backend`. |
| `backend/railway.json` | Removed `startCommand` (Dockerfile CMD owns it). Healthcheck timeout 60→120, restart retries 3→5, numReplicas=1. |
| `backend/nixpacks.toml` | Marked deprecated, kept for legacy services that still resolve nixpacks. |
| `backend/.dockerignore` | New — keeps venv, dev.db, cookies.txt out of build context. |
| `backend/services/transcript.py` | Added `YT_PROXY` env (falls back to HTTPS_PROXY/HTTP_PROXY). Added `_retryable()` + `_backoff_sleep()`. Wrapped `_download_subtitle` and `_ytdlp_fetch` in retry loops with jittered exponential backoff for 408/425/429/500/502/503/504/529 + network errors. yt-dlp `_YDL_OPTS` now includes `proxy`, `retries=4`, `socket_timeout=30`. |
| `frontend/Dockerfile` | New — multi-stage (deps → builder → runner) standalone build. `HOSTNAME=0.0.0.0` so it binds correctly on Railway. |
| `frontend/.dockerignore` | New — excludes node_modules, .next, env files. |
| `frontend/railway.json` | Switched from NIXPACKS to DOCKERFILE builder. Removed startCommand. |
| `.env.example` | Added `ALLOWED_ORIGINS`, `YT_PROXY`, `YT_MAX_RETRIES`, `YT_BASE_BACKOFF`. |
| `DEPLOY.md` | New — Railway setup guide. Three services (backend, frontend, postgres), private-network reference vars (`${{backend.RAILWAY_PRIVATE_DOMAIN}}`), auto-deploy on push, 529 playbook, cost expectation. References session 13 us-west2 Legacy proxy issue and recommends re-provision in modern region. |

Tests run: AST parse on `transcript.py` ok. No runtime test — verification happens when Railway builds the Dockerfiles.

Verification path (Rakshan does this in Railway dashboard):
1. Backend logs show `Running upgrade -> 001, initial schema` then `Uvicorn running on http://0.0.0.0:$PORT`.
2. `curl https://<backend>.up.railway.app/health` → `{"status":"ok","service":"omi-ted-v2"}`.
3. Frontend root URL renders OMI-TED v2 header + amber banner.

## What's Next (next session starts here)

### Step 1 — Re-provision Railway (one-time, ~15 min)

Open `DEPLOY.md` and follow sections 1-6. Need:
- Railway account (linked GitHub already)
- 3 API keys: Sarvam (has credits), OpenRouter (no credits — translation will fail until added), Google Translate
- Pick a non-`us-west2` region (us-east4 / eu-west1) to avoid the proxy routing bug from session 13

### Step 2 — Smoke test

```bash
curl https://<backend>.up.railway.app/health
# expect: {"status":"ok","service":"omi-ted-v2"}

curl -X POST https://<backend>.up.railway.app/api/v1/ingest/ \
  -H "content-type: application/json" \
  -d '{"youtube_id":"<paste a Telugu sermon ID>"}'
# expect: 202 with {"job_id": ..., "status": "queued"}
```

### Step 3 — Continue M1 work

Record success rate on 20 IDs from Excel using the Railway backend (cleaner IPs than residential). If <70%, set `YT_PROXY` (Webshare ~$1/mo) and retry. Then move to M3 editor UI hardening (already mostly built per session 04).

### Test command to verify pickup works

```bash
git pull --rebase origin main
git log -1 --pretty=%s   # should show "deploy: railway always-on configs ..."
# Then open Railway dashboard, confirm both services Active (green dot)
```

## Blockers

- **Railway account / GitHub link** — already linked. Just need to re-provision the project.
- **3 API keys** — Sarvam OK, OpenRouter has $0 credit (translation pipeline will fail until topped up), Google Translate must be confirmed live.
- **Webshare proxy** — defer until 529s show up.

## Notes for Next Session

- **Local-first check should be restored** - run `uvicorn main:app --reload` for development and testing before Railway/Vercel deploys; use Railway preview/private networking only as a secondary check.
- **Migrations are automatic** — every backend boot runs `alembic upgrade head`.
- **Frontend ↔ backend over Railway private network** (`RAILWAY_PRIVATE_DOMAIN`). No public hop, no CORS.
- **Session 13 left the existing `capable-victory` project broken on us-west2 Legacy.** Easier to re-provision than debug. DEPLOY.md step 2 says so.
- **`backend/nixpacks.toml`** is intentionally a deprecation stub. Delete it once Railway logs confirm DOCKERFILE builder.
- **`youtube-transcript-api`** stays at >=0.6.2 (pre-1.0 classmethod API). yt-dlp+proxy is the real path on Railway.
- **Windows/NTFS troubleshooting** - prefer Git for Windows or a WSL2 ext4 clone for edits. If `/mnt/c` file operations corrupt files, reproduce with logs, then use a temporary clean clone only as a workaround.

---

## Pickup Protocol
1. `git pull --rebase origin main`
2. Read this file
3. Open Railway dashboard — confirm both services Active
4. Run the smoke-test commands above
5. If green, continue M1 (`backend/services/transcript.py` is hardened; just need real-world success rate data)

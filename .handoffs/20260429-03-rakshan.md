# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 03
- **Machine**: rakshan
- **Date/time started**: 2026-04-29
- **Date/time ended**: 2026-04-29
- **Context tokens used (approx)**: ~25%
- **Model used**: claude-opus-4-7

## Repo State
- **Branch**: main
- **Last commit SHA**: (run `git rev-parse HEAD` after this push)
- **Last commit message**: deploy: railway always-on configs (auto-migrate, retry, proxy)
- **Uncommitted changes**: see `git status --short` after edits — DEPLOY.md, backend/Dockerfile, backend/railway.json, backend/.dockerignore, backend/services/transcript.py, frontend/Dockerfile, frontend/.dockerignore, frontend/railway.json, .env.example, .handoffs/20260429-03-rakshan.md
- **Stash entries**: none

## Local Environment State
- **Backend running**: no — local skipped this session, deploy is cloud-only now
- **Frontend running**: no — same
- **DB in use**: Postgres on Railway (managed plugin)
- **Migration version**: auto-applied on every container boot via Dockerfile CMD
- **Row counts**: 0 (Postgres fresh)
- **LLM provider in config.yaml**: sarvam
- **Sentry/PostHog connected**: no

## What I Just Completed

### Switched project from local-first to always-on Railway

Per Rakshan's request — local kept hitting `backend not connected`, `yt-dlp 529`, and generic `network error` on Windows. Decision: skip local debug, deploy 24/7 cloud on Railway (full-stack: backend + frontend + Postgres). Auto-deploy on every push to `main`.

Files touched:

| File | Change |
|------|--------|
| `backend/Dockerfile` | Context-relative COPY (was `COPY backend/...`, now `COPY ...`). CMD chains `alembic upgrade head` before `uvicorn` so migrations run on every boot. Added `PYTHONUNBUFFERED=1`, workers=2. |
| `backend/railway.json` | Removed `startCommand` (Dockerfile CMD owns it). Healthcheck timeout 60→120, restart retries 3→5. |
| `backend/nixpacks.toml` | Marked deprecated, kept for legacy services that still resolve nixpacks. |
| `backend/.dockerignore` | New — keeps venv, dev.db, cookies.txt out of build context. |
| `backend/services/transcript.py` | Added `YT_PROXY` env (falls back to HTTPS_PROXY/HTTP_PROXY). Added `_retryable()` + `_backoff_sleep()`. Wrapped both `_download_subtitle` and `_ytdlp_fetch` in retry loops with jittered exponential backoff for 408/425/429/500/502/503/504/529 + network errors. yt-dlp `_YDL_OPTS` now includes `proxy`, `retries=4`, `socket_timeout=30`. |
| `frontend/Dockerfile` | New — multi-stage (deps → builder → runner) standalone build. `HOSTNAME=0.0.0.0` so it binds correctly on Railway. |
| `frontend/.dockerignore` | New — excludes node_modules, .next, env files. |
| `frontend/railway.json` | Switched from NIXPACKS to DOCKERFILE builder. Removed startCommand (Dockerfile CMD owns it). |
| `.env.example` | Added `ALLOWED_ORIGINS`, `YT_PROXY`, `YT_MAX_RETRIES`, `YT_BASE_BACKOFF`. |
| `DEPLOY.md` | New — full Railway setup guide. Three services (backend, frontend, postgres), private-network reference vars (`${{backend.RAILWAY_PRIVATE_DOMAIN}}`), auto-deploy on push, 529 playbook, cost expectation. |

Tests run: none — file edits only. Real verification happens when Railway builds the Dockerfiles after `git push`.

Verification path (Rakshan does this on Railway dashboard, not Cowork):
1. Backend logs: `Running upgrade  -> 001, initial schema` then `Uvicorn running on http://0.0.0.0:$PORT`
2. `curl https://<backend>.up.railway.app/health` → `{"status":"ok","service":"omi-ted-v2"}`
3. Frontend root URL renders OMI-TED v2 header + amber banner.

## What's Next (next session starts here)

### Step 1 — Push and connect Railway (one-time, ~15 min)

Open `DEPLOY.md` and follow sections 1-6. Need:
- Railway account (free signup, link GitHub)
- The 3 API keys you confirmed (Sarvam, OpenRouter, Google)
- A Webshare proxy URL **only if** ingest 529s persist after retry — skip for first deploy

### Step 2 — Smoke test

```bash
# From any machine, no local setup needed
curl https://<backend>.up.railway.app/health
# expect: {"status":"ok","service":"omi-ted-v2"}

curl -X POST https://<backend>.up.railway.app/api/v1/ingest/ \
  -H "content-type: application/json" \
  -d '{"youtube_id":"<paste a Telugu sermon ID>"}'
# expect: 202 with {"job_id": ..., "status": "queued"}
```

### Step 3 — Continue M1 work

Now that ingest runs on Railway IPs (cleaner than residential), record the success rate on 20 IDs. If <70%, set `YT_PROXY` and retry. Then move to M3 editor UI.

### Test command to verify pickup works

```bash
git pull --rebase origin main
git log -1 --pretty=%s   # should show "deploy: railway always-on configs ..."
# Then open Railway dashboard, confirm both services are "Active" (green dot)
```

## Blockers

- **Railway account + GitHub link** — Rakshan must do this in browser (Cowork can't auth Railway from inside the sandbox).
- **3 API keys** — confirmed available. Paste into Railway backend service Variables tab per `DEPLOY.md` Step 4.
- **Webshare proxy** — defer until 529s show up; not needed for first deploy.

## Notes for Next Session

- **No more `uvicorn main:app --reload`** locally for this project. If you want to test, just push to a feature branch and deploy a Railway preview environment.
- **Migrations are automatic now** — every backend boot runs `alembic upgrade head`. Don't run migrations manually.
- **Frontend talks to backend over Railway private network** (`RAILWAY_PRIVATE_DOMAIN`). No public-internet hop, no CORS issues.
- **`backend/nixpacks.toml`** is intentionally kept as a deprecation stub. Delete it once Railway logs confirm DOCKERFILE builder is used.
- **`cookies.txt` workflow is local-only** — on Railway, prefer `YT_PROXY` over cookies (cookies expire and need maintenance).
- **`youtube-transcript-api`** is currently >=0.6.2 in requirements. Still uses pre-1.0 classmethod API (`YouTubeTranscriptApi.list_transcripts`). If Railway IPs hit `RequestBlocked`, the yt-dlp fallback (with proxy) takes over — that's the path designed to work.

---

## Pickup Protocol (read this when starting on the other machine)
1. `git pull --rebase origin main`
2. Read this file (`.handoffs/20260429-03-rakshan.md`)
3. Open Railway dashboard — confirm both services Active
4. Run the test command above
5. If green, continue M1 (`backend/services/transcript.py` is hardened; just need real-world success rate data)

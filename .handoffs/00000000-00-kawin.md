# OMI-TED v2 — Session 0 (Scaffold)
*Pre-filled starting state. From Session 1 onwards, write `.handoffs/YYYYMMDD-NN-{kawin|rakshan}.md` from `HANDOFF-TEMPLATE.md`.*

---

## Session Identity
- **Session #**: 0
- **Machine**: kawin (initial scaffold)
- **Date**: 2026-04-25
- **Model used**: claude-sonnet-4-6
- **Context tokens used**: N/A (this file is pre-written, not session output)

## Repo State
- **Branch**: main
- **Last commit**: initial scaffold (this file + project-settings.md + CLAUDE.md + sync infra)
- **Uncommitted changes**: none (everything in this commit)

## Local Environment State
- **Backend**: not yet scaffolded
- **Frontend**: not yet scaffolded
- **DB**: none (SQLite `dev.db` will be created by first migration in M0)
- **LLM provider in config.yaml**: not yet (will be `sarvam` in M0)

## What I Just Completed
- Repo created at `C:\Users\<user>\Projects\omi-ted-v2`
- Files in repo root: `CLAUDE.md`, `project-settings.md`, `HANDOFF-TEMPLATE.md`, `SYNC-WORKFLOW.md`, `setup.ps1`, `.gitignore`, `.env.example`
- `.handoffs/` folder created with this Session 0 file
- Pushed to `github.com/rakshan-rakshan/omi-ted-v2`

## What's Next (Session 1 starts here)

**Pre-flight (must run before M1)**:

1. Create `backend/scripts/validate_transcripts.py` with the script in `project-settings.md` → Pre-Code Validation section
2. Pick 20 representative URLs from your Excel sheet
3. Run the script
4. Record success rate in the Session 1 handoff

**Test command**:
```powershell
cd backend
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install youtube-transcript-api
python scripts\validate_transcripts.py
```

**Decision point at end of pre-flight**:
- ≥70% success → proceed to M0 scaffold, then M1 Transcript Ingest
- <70% success → stop. Validate yt-dlp fallback path before writing M1.

After pre-flight: M0 (full repo scaffold — backend skeleton, frontend skeleton, Alembic init, /health endpoint) → M1.

## Blockers
- None.

## Notes
- v1 (`omi-automation`) is not carried forward. Sessions 1–7 of v1 do not count toward v2 progress.
- Local-first rule is in force: no Railway, no Vercel until M1–M4 work locally with real data.
- If Cowork hits 95% context during pre-flight or M0, stop and hand off — these aren't tasks worth pushing through context exhaustion.

# Handoff — 2026-04-27 · Session 12 · rakshan

## COMMUNICATION STYLE FOR NEXT SESSION
Caveman mode. Short. Direct. Do the thing.

## WHERE WE ARE
Railway deploy is IN PROGRESS (or just completed). Two bugs were squashed mid-session:
1. `backend/Dockerfile` was doing `COPY requirements.txt .` — fixed to `COPY backend/requirements.txt .` and `COPY backend/ .` (build context = repo root, not backend/)
2. `backend/requirements.txt` had trailing null bytes from Windows NTFS — cleaned

Latest pushed commit: `658c188` on `main`

**Railway project**: `capable-victory`
**Service**: `omi-ted-v2` (backend only so far)
**URL**: https://railway.com/project/55535d1f-eb9e-4afa-b2ef-1bded23fb515

## WHAT WAS DONE THIS SESSION

| What | Detail |
|------|--------|
| Git commit sessions 05-11 | All accumulated changes pushed (40 files, 3744 insertions) |
| CORS fix | `backend/main.py` — reads `ALLOWED_ORIGINS` env var, also allows `*.up.railway.app` via regex |
| `next.config.js` | Added `output: "standalone"` for Railway frontend deploy |
| `frontend/railway.json` | Start command = `node .next/standalone/server.js` |
| Railway builder set to Dockerfile | Settings → Build → Dockerfile → `/backend/Dockerfile` |
| Env vars set in Railway | `DATABASE_URL`, `SARVAM_API_KEY`, `OPENROUTER_API_KEY`, `DEBUG=false` |
| Git push workaround | NTFS mount breaks `git add`; solution = clone to `/tmp/omi-push` and push from there |
| GitHub token in remote URL | `git remote set-url origin https://$GITHUB_TOKEN@github.com/...` |

## NEXT SESSION: FINISH RAILWAY DEPLOY

### Task 1 — Verify backend is live
Check Railway Deployments tab. If green:
- Go to Settings → Networking → Generate Domain
- Hit `https://<domain>/health` — should return `{"status":"ok"}`
- If still failing, check build logs for new error

### Task 2 — Add PostgreSQL (replace SQLite)
Railway dashboard → `capable-victory` project → **+ New** → **Database** → **PostgreSQL**

Then in backend service → Variables:
- Change `DATABASE_URL` from `sqlite+aiosqlite:///./dev.db` to `${{Postgres.DATABASE_URL}}`

SQLite works for dev but resets on every Railway redeploy (ephemeral filesystem).

### Task 3 — Deploy frontend
Back in Railway project canvas → **+ New** → **GitHub Repo** → `rakshan-rakshan/omi-ted-v2` → pick `frontend` folder

Set env var on frontend service:
```
BACKEND_URL=http://<backend-internal-railway-hostname>:8000
```
(Railway gives internal hostname in the backend service's Settings → Networking)

### Task 4 — Expose the services
Backend: Settings → Networking → Generate Domain
Frontend: same — this becomes the public URL

### Task 5 — Smoke test
- Open frontend URL → should load the dashboard
- Paste a YouTube Telugu video URL → should ingest without cookies (Railway IPs work)

## KNOWN ISSUES / GOTCHAS

| Issue | Status |
|-------|--------|
| `git add` fails from Linux sandbox on NTFS mount | Workaround: `git clone` to `/tmp/omi-push`, edit there, push from there |
| `.git/index.lock` stale on Windows | Delete `C:\Users\kawin\Projects\omi-ted-v2\.git\index.lock` manually if needed |
| OpenRouter has no credits yet | Translation won't work but ingest + display will |
| Railway free tier ~$0.99 left | May need to add $5 credit before backend stays alive |
| `backend/requirements.txt` had null bytes | Fixed — clean version in repo now |
| Frontend not deployed yet | Only backend service exists so far |

## STATE VERIFY ON START
```bash
# Check latest commits are on GitHub
cd /tmp/omi-push && git pull && git log --oneline -3

# Check Railway backend status (use Chrome → railway.com/dashboard)
# Project: capable-victory → omi-ted-v2 service → Deployments tab
```

## CREDENTIALS (don't commit these)
- GitHub auth: use `gh auth login`, SSH, or `GITHUB_TOKEN` from a secure vault.
- OpenRouter: set `OPENROUTER_API_KEY` from the secure vault.
- Sarvam: set `SARVAM_API_KEY` from the secure vault.

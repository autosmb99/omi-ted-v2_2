# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 01
- **Machine**: rakshan
- **Date/time started**: 2026-04-25 (Cowork session)
- **Date/time ended**: 2026-04-25 (paused at ~90% context)
- **Context tokens used (approx)**: ~90%
- **Model used**: claude-opus-4-7

## Repo State
- **Branch**: main
- **Last commit SHA**: (run `git rev-parse HEAD` and paste)
- **Last commit message**: chore: initial v2 scaffold
- **Uncommitted changes** (after this session — verify with `git status --short`):
```
M  .gitignore                                  (added cookies.txt)
A  .env.example
A  .handoffs/20260425-01-rakshan.md            (this file)
M  .handoffs/00000000-00-kawin.md              (moved from repo root)
M  project-settings.md                          (Pre-Code Validation snippet patched to v1.x)
A  backend/requirements.txt
A  backend/main.py
A  backend/config.yaml
A  backend/database.py
A  backend/models.py
A  backend/routers/__init__.py
A  backend/routers/health.py
A  backend/services/__init__.py
A  backend/alembic.ini
A  backend/alembic/env.py
A  backend/alembic/script.py.mako
A  backend/alembic/versions/001_initial_schema.py
A  backend/scripts/validate_transcripts.py
A  backend/scripts/validate_transcripts_ytdlp.py
```
- **Stash entries**: none

## Local Environment State
- **Backend running**: no — never been started yet
- **Frontend running**: no — frontend scaffold not yet written (next session)
- **DB in use**: none — `alembic upgrade head` not yet run, so `dev.db` doesn't exist
- **Migration version**: revision `001` exists in `backend/alembic/versions/` but not applied
- **Row counts**: 0 across the board (no DB)
- **LLM provider in `config.yaml`**: `sarvam` (default — not yet wired into code; M5)
- **Sentry/PostHog connected**: no

## What I Just Completed

### Pre-flight (M-1)

| Path | Result | Notes |
|------|--------|-------|
| `youtube-transcript-api` v1.2.4 | **0/20 (0%)** | All hit `RequestBlocked` — YouTube IP-bans the residential IP for unauthenticated transcript fetches. |
| `yt-dlp` no auth | **0/20 (0%)** | All hit "Sign in to confirm you're not a bot". |
| `yt-dlp` + Chrome cookies | **0/20 (0%)** | Chrome 127+ App-Bound Encryption blocks yt-dlp from reading the cookie DB (yt-dlp issue #7271). |
| `yt-dlp` + Edge / Firefox / cookies.txt | **NOT YET RUN** | Script will auto-fall-through; you stopped before running this leg. |

**Spec drift fix**: `project-settings.md` Pre-Code Validation snippet was written against `youtube-transcript-api` pre-1.0 (`YouTubeTranscriptApi.list_transcripts` classmethod). Library is now v1.2.4, classmethod is gone. **Snippet patched to v1.x API + a note about the IP-block failure mode.** Don't revert.

### M0 — Backend skeleton (done)

All files created and `ast.parse`-clean:
- `backend/main.py` — FastAPI app, mounts `/health`. Future routers commented out as a roadmap.
- `backend/database.py` — async SQLAlchemy engine, session factory, `Base`, FastAPI `get_session` dependency.
- `backend/models.py` — `Video`, `Segment`, `Job`, `GlossaryTerm` matching the canonical schema in `project-settings.md`.
- `backend/routers/health.py` — `GET /health` → `{"status": "ok", "service": "omi-ted-v2"}`.
- `backend/config.yaml` — LLM provider config (sarvam default), song heuristic threshold.
- `backend/requirements.txt` — fastapi 0.115, sqlalchemy 2.0 + aiosqlite + asyncpg, alembic 1.14, pydantic 2.10, youtube-transcript-api 1.2.4, yt-dlp 2024.12.13.
- `backend/alembic.ini` + `backend/alembic/env.py` (async-aware, reads `DATABASE_URL` from env) + `backend/alembic/script.py.mako`.
- `backend/alembic/versions/001_initial_schema.py` — creates videos, segments, jobs, glossary tables with all indices.

### M0 — Frontend skeleton (NOT done)

Stopped before scaffolding `frontend/`. Next session.

### Validation script hardening

`backend/scripts/validate_transcripts_ytdlp.py` now auto-cascades cookie sources:
1. `$env:YTDLP_COOKIES_FILE` if set (path to cookies.txt)
2. `$env:YTDLP_COOKIES_BROWSER` if set (forced single browser)
3. Otherwise probes Edge → Firefox → Chromium → Chrome → Brave, picks the first one that successfully fetches video metadata, then runs the full 20-video sweep.

`cookies.txt` is in `.gitignore`.

## What's Next (next session starts here)

### Step A — finish pre-flight

```powershell
cd C:\Users\kawin\Projects\omi-ted-v2\backend ; .\venv\Scripts\Activate.ps1 ; python scripts\validate_transcripts_ytdlp.py
```

Close all browsers first (yt-dlp needs an exclusive read on the cookie DB). The script picks the first working browser automatically.

If all browsers fail, manual fallback (5 minutes):
1. Chrome extension: **"Get cookies.txt LOCALLY"**, sign into youtube.com, click Export
2. Save to `C:\Users\kawin\Projects\omi-ted-v2\cookies.txt`
3. `$env:YTDLP_COOKIES_FILE='C:\Users\kawin\Projects\omi-ted-v2\cookies.txt' ; python scripts\validate_transcripts_ytdlp.py`

Decision tree:
- ≥70% videos return Telugu auto-captions → record number in handoff, proceed to Step B.
- <70% → investigate Webshare proxy support in `youtube-transcript-api` (built-in) before M1.

### Step B — verify M0 backend boots

```powershell
cd C:\Users\kawin\Projects\omi-ted-v2\backend
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
alembic upgrade head     # creates dev.db with the 4-table schema
uvicorn main:app --reload
```

Then in another shell:

```powershell
curl http://localhost:8000/health
```

Expected: `{"status":"ok","service":"omi-ted-v2"}`. If green, M0 backend is verified.

### Step C — M0 frontend scaffold (not yet written)

Next session needs to create:
- `frontend/package.json` — Next 14, React 18, TanStack Table v8, Zustand, SWR, axios
- `frontend/next.config.js` — `rewrites` proxy `/api/:path*` → `http://localhost:8000/api/:path*` (eliminates CORS, per v1 lessons)
- `frontend/tsconfig.json`
- `frontend/app/layout.tsx`, `frontend/app/page.tsx` (placeholder home → video list), `frontend/app/globals.css`
- `frontend/.env.local.example` — `NEXT_PUBLIC_API_BASE=http://localhost:8000`

Verify with `npm install ; npm run dev` → http://localhost:3000 loads.

### Step D — README.md + commit + push

```powershell
git add -A ; git commit -m "session 01: pre-flight + M0 backend + handoff" ; git push origin main
```

### Test command to verify pickup works

```powershell
cd C:\Users\kawin\Projects\omi-ted-v2\backend ; .\venv\Scripts\Activate.ps1 ; pip install -r requirements.txt ; alembic upgrade head ; uvicorn main:app --reload
```

Then `curl http://localhost:8000/health` should return `{"status":"ok","service":"omi-ted-v2"}`.

## Blockers

- **YouTube auth wall**. Until Step A succeeds, M1 (Transcript Ingest) cannot be designed concretely. M0 backend can still be verified independently — no YouTube call needed for `/health`.

## Notes for Next Session

- **`project-settings.md` Pre-Code Validation snippet is now v1.x-correct.** Don't revert.
- **Cowork sandbox limitations**: my Linux sandbox can't (a) reach `youtube.com` (egress proxy 403) or (b) read Windows browser cookie DBs or (c) perform git operations against the Windows-mounted `.git/`. All YouTube-touching, browser-cookie, and git/push operations must run from PowerShell on the Windows side. File-only operations (Read/Write/Edit + Python file creation) work fine through the mount.
- **`gh` CLI not installed**; remote `origin` was added manually. Push verification was done by the user (output not pasted).
- **Folder convention**: handoffs live at `.handoffs/` (with the dot). Don't accidentally create `handoffs/`.
- **`.gitignore` additions**: `cookies.txt` and `**/cookies.txt` to keep YouTube session cookies out of the repo.
- **M1 implication if yt-dlp+cookies path is the only one that works**: the M1 plan in `project-settings.md` says "Fetch te + en transcripts via youtube-transcript-api / Fallback: yt-dlp if library fails" — that order may need to flip (yt-dlp primary, library secondary), since the library has been failing 100% on residential IPs. Note this when designing M1.
- **Dependency note**: `requirements.txt` pins `youtube-transcript-api==1.2.4` and `yt-dlp==2024.12.13`. If you bump yt-dlp later, re-test the cookie cascade — yt-dlp's cookie code changes often.

---

## Pickup Protocol (read this when starting on the other machine)
1. `git pull --rebase origin main`
2. Read the most recent file in `.handoffs/` (this file)
3. Run the test command in "What's Next → Test command to verify pickup works" — confirm `/health` is green
4. Then run Step A (yt-dlp validation) — record success rate
5. Then Step C (frontend scaffold) → Step D (commit + push)
6. Begin

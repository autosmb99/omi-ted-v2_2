# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 13
- **Machine**: rakshan
- **Date/time started**: 2026-04-27 ~18:30 IST
- **Date/time ended**: 2026-04-27 ~20:00 IST
- **Context tokens used (approx)**: ~95% (context compaction triggered mid-session)
- **Model used**: claude-sonnet-4-6

## Repo State
- **Branch**: main
- **Last commit SHA**: ec5630756cb7509e0519d3a00af34420f65b3128
- **Last commit message**: fix: remove EXPOSE 8000 — let Railway route to its own PORT env var
- **Uncommitted changes**: none (all pushed via /tmp/omi-push — see Notes)
- **Stash entries**: none

> Workflow note: prefer Git for Windows or a WSL2 ext4 clone for edits. See `project-settings.md` for the current Windows/WSL guidance.

## Local Environment State
- **Backend running**: no
- **Frontend running**: no
- **DB in use**: SQLite (`./dev.db`)
- **Migration version**: unknown (not checked this session)
- **Row counts**: unknown
- **LLM provider in config.yaml**: sarvam (has credits), openrouter (no credits yet)
- **Sentry/PostHog connected**: no

## What I Just Completed

All fixes were pushed from `/tmp/omi-push` (fresh clone) to bypass NTFS corruption:

### Files Fixed (NTFS corruption root cause)
- **`backend/main.py`** — was truncated at line 38 due to NTFS null-byte corruption committed to GitHub. Completely rewritten with all 7 routers intact.
- **`backend/requirements.txt`** — had hundreds of null bytes on line 14. Rewritten clean with 13 packages.
- **`backend/Dockerfile`** — Three fixes applied:
  1. `COPY requirements.txt .` → `COPY backend/requirements.txt .` (build context is repo root)
  2. `COPY . .` → `COPY backend/ .`
  3. Removed `EXPOSE 8000` and `ENV PORT=8000`; CMD now uses `${PORT:-8000}`
- **`frontend/next.config.js`** — Added `output: "standalone"` for Railway deployment
- **`frontend/railway.json`** — Added start command `node .next/standalone/server.js`

### Railway Deployment Status
- Railway builder: manually set to **Dockerfile** (Settings → Build)
- Active deployment: `af797a08` (commit `ec56307`)
- Deploy logs confirm: `Uvicorn running on http://0.0.0.0:8080` ✓
- Networking settings confirm: `omi-ted-v2-production.up.railway.app → Port 8080` ✓
- Service shows: **Online** in dashboard
- **BUT**: URL `<RAILWAY_SERVICE_URL>` returns "site can't be reached"
- HTTP logs show: **zero traffic** — Railway's proxy is not forwarding requests to the container
- Service goes **Sleeping** after ~7 min idle (no traffic = confirmed proxy issue)

## What's Next (next session starts here)

**Immediate blocker**: Railway proxy not routing traffic to backend despite correct config.

### Diagnosis steps to try in order:

1. **Try explicitly setting `PORT=8080` as a Railway Variable** (Variables tab → add `PORT` = `8080`). Then redeploy. This forces Railway to know which port to route to without relying on inference.

2. **Check if us-west2 (Legacy) is the issue** — In Settings → Deploy, there may be an option to re-provision on a newer region. Railway deprecated us-west2. Try creating a new service from scratch in US West or US East region pointing to the same GitHub repo.

3. **Add a health check URL** — In Settings → Deploy → Health Check Path, set `/health`. This lets Railway verify the service is up before marking Active.

4. **If all else fails**: Delete the service and re-create it from scratch in Railway, selecting the Dockerfile builder and a non-legacy region.

### After backend URL works:
5. Add PostgreSQL service to Railway project (SQLite resets on every deploy)
6. Deploy frontend as second Railway service; set `BACKEND_URL` env var to backend's internal Railway URL (e.g. `http://omi-ted-v2.railway.internal:8080`)
7. Smoke test: paste a Telugu YouTube URL at `/api/v1/ingest/youtube` — should work without cookies.txt from Railway's IPs
8. Add OpenRouter credits (currently $0 — translation won't work until credits added)

### First action next session:
Go to Railway dashboard → omi-ted-v2 service → Variables → add `PORT = 8080` → redeploy → test URL.

## Blockers
- Railway URL `<RAILWAY_SERVICE_URL>` unreachable despite backend running correctly on port 8080. Root cause unknown — likely us-west2 legacy region proxy routing issue.
- OpenRouter has no credits → translation pipeline will fail even after deploy is fixed.

## Notes for Next Session

### CRITICAL: Git workflow due to NTFS corruption
- **Never edit files at the Windows mount path** (`/sessions/fervent-gifted-pascal/mnt/omi-ted-v2/`) from bash — NTFS truncates/corrupts Python files silently.
- **Safe workflow**:
  ```bash
  git clone https://$GITHUB_TOKEN@github.com/rakshan-rakshan/omi-ted-v2 /tmp/omi-push
  cd /tmp/omi-push
  # make edits here
  git add -A && git commit -m "message"
  git push origin main
  ```
- GitHub auth: use `gh auth login`, SSH, or `GITHUB_TOKEN` from a secure vault.
- The workspace mount (`C:\Users\kawin\Projects\omi-ted-v2`) will be stale — that's fine, Railway deploys from GitHub directly.

### API Keys
- Sarvam: set `SARVAM_API_KEY` from the secure vault.
- OpenRouter: set `OPENROUTER_API_KEY` from the secure vault.

### Railway project
- Project: `capable-victory` / `production`
- Service URL: `<RAILWAY_SERVICE_URL>`
- Project ID: `<RAILWAY_PROJECT_ID>`
- Service ID: `<RAILWAY_SERVICE_ID>`

---

## Pickup Protocol
1. `git clone https://TOKEN@github.com/rakshan-rakshan/omi-ted-v2 /tmp/omi-push && cd /tmp/omi-push && git log --oneline -3`
2. Go to Railway dashboard → Variables tab → add `PORT=8080` → redeploy
3. Wait 2 min → test `<RAILWAY_SERVICE_URL>/health`
4. If still broken, re-create Railway service in non-legacy region
5. Once working, proceed to PostgreSQL + frontend deploy

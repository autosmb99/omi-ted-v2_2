# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 04
- **Machine**: rakshan
- **Date/time started**: 2026-04-26
- **Date/time ended**: 2026-04-26
- **Context tokens used (approx)**: ~90%
- **Model used**: claude-sonnet-4-6

## Repo State
- **Branch**: main
- **Last commit SHA**: (run `git rev-parse HEAD`)
- **Last commit message**: session 03-04: M2+M3+M4 segments API + editor + export [rakshan]
- **Uncommitted changes**: none after push
- **Stash entries**: none

## Local Environment State
- **Backend running**: yes — port 8000, `--reload` active
- **Frontend running**: not yet — `npm install` needed then `npm run dev`
- **DB in use**: SQLite `backend/dev.db`
- **Migration version**: revision `001`
- **Row counts**: videos=3, segments=1377 (video 3 = qffQo0GG4hs)
- **LLM provider**: sarvam (not wired — M5)
- **Sentry/PostHog**: no

## What I Just Completed

### M2 — Video list + segments API
`backend/routers/videos.py`:
- `GET /api/v1/videos` — list all videos with segment counts
- `GET /api/v1/videos/{youtube_id}/segments?page=1&page_size=50` — paginated segments
- `PATCH /api/v1/segments/{id}` — save en_human + is_reviewed + quality_score; auto-sets en_final

`backend/routers/export.py`:
- `GET /api/v1/export/jsonl?youtube_id=X` — streaming JSONL download
- `GET /api/v1/export/jsonl?youtube_id=X&reviewed_only=true` — reviewed segments only
- `GET /api/v1/export/csv?youtube_id=X` — CSV with all fields

`backend/main.py` — both routers mounted, CORS middleware added for localhost:3000

### M3 — Parallel editor frontend
`frontend/components/ParallelEditor.tsx`:
- TanStack Table v8 — 4 columns: Time | Telugu | Auto English | Your Translation
- Editable textarea in "Your Translation" column
- Saves on blur or Ctrl+Enter via `PATCH /api/v1/segments/{id}`
- "saving…" / "saved ✓" micro-feedback per cell
- Reviewed rows highlighted green

`frontend/app/editor/[videoId]/page.tsx`:
- Loads segments via SWR with optimistic local cache update
- Pagination (50 per page, prev/next buttons)
- Shows: total segments, page X/Y, reviewed count this page
- Export buttons in header: JSONL / JSONL (reviewed) / CSV — direct download links

`frontend/app/page.tsx`:
- Live video list via SWR (refreshes every 5s)
- Status pill (fetched/fetching/pending/error/no_transcript)
- "Edit →" link for fetched videos → `/editor/{youtube_id}`

### Infrastructure fixes
- `asyncpg` commented out of requirements.txt (no Python 3.12 wheels without C compiler)
- `transcript.py` truncation bug fixed (write via bash cp instead of Write tool)
- `PYTHONPYCACHEPREFIX=/tmp/pycache` needed in Cowork sandbox to bypass stale .pyc on Windows mount

## What's Next

The core loop is now working:
1. Ingest a video → `POST /api/v1/ingest/`
2. Open editor → http://localhost:3000/editor/{youtube_id}
3. Correct translations in the "Your Translation" column
4. Export → click JSONL or CSV button

### To boot the full stack

**Backend** (terminal 1):
```powershell
cd C:\Users\kawin\Projects\omi-ted-v2\backend
.\venv\Scripts\Activate.ps1
$env:YTDLP_COOKIES_FILE='C:\Users\kawin\Projects\omi-ted-v2\cookies.txt'
python -m uvicorn main:app --reload
```

**Frontend** (terminal 2 — first time only needs npm install):
```powershell
cd C:\Users\kawin\Projects\omi-ted-v2\frontend
npm install
npm run dev
```

Open http://localhost:3000 — video list loads. Click "Edit →" on qffQo0GG4hs to open the editor.

### Next module: M5 — LLM translation (optional)

If the YouTube auto-English is too rough to be useful, M5 fills `en_auto` via Sarvam or Google Translate:
- `backend/services/translate.py` — `translate(text, src, tgt) -> str` using provider from `config.yaml`
- `backend/routers/batch.py` — `POST /api/v1/batch/translate?youtube_id=X` — fills en_auto for all segments where it's null
- Frontend: "Auto-translate" button on editor page

## Blockers
- None. Boot the stack and start editing.

## Notes for Next Session
- **Python must be 3.12**. Venv at `backend/venv`. Always activate before running anything.
- **YTDLP_COOKIES_FILE** must be set in the terminal running uvicorn.
- **Invoke-WebRequest** not `curl` for PowerShell API calls.
- **`asyncpg` is commented out** — don't uncomment until Railway.
- **Write tool truncates large files** on the Windows mount — use bash `cp` from `/tmp` for files >100 lines if issues arise.
- **en_final logic**: set by `PATCH /api/v1/segments/{id}` automatically — en_human if set, else en_auto. Don't set it manually.

---

## Pickup Protocol
1. `git pull --rebase origin main`
2. Read this file
3. Boot backend + frontend (commands above)
4. Open http://localhost:3000 — video list should show qffQo0GG4hs with 1377 segments
5. Click "Edit →" — editor loads with Telugu + English columns
6. Begin editing or ingest more videos
